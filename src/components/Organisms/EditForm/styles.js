import { StyleSheet } from "aphrodite/no-important";

export default StyleSheet.create({
  form: {
    maxWidth: 400,
    margin: "0 auto",
    display: "flex",
    alignItems: "center"
  },

  row: {
    margin: "20px 0"
  }
});
