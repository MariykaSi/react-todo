import { StyleSheet } from "aphrodite/no-important";

export default StyleSheet.create({
  form: {
    maxWidth: 400,
    margin: "0 auto",
    display: "flex",
    alignItems: "center",
    flexDirection: "column"
  },

  row: {
    width: "100%",
    display: "flex",
    alignItems: "center",
    margin: "20px 0"
  }
});
